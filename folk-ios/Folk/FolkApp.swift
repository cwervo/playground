// FolkApp.swift
// Folk for iOS — a Swift VM for the Folk language: a Tcl evaluator with
// Folk's Claim / Wish / When on top, and a Metal Surface standing in for
// Folk's Vulkan render context.

import SwiftUI

@main
struct FolkApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
