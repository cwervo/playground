When $this has region /r/ {
    set angle [region angle $r]
    set step [expr { 1 + round(14 * abs($angle / 3.1415)) }]
    Wish $this is titled "Frames: $step"

    Claim animation binder's frame count is $step
}
